[pytest]
addopts = -p no:warnings
filterwarnings =
    ignore::DeprecationWarning
markers = 
    pandas: marks tests as a pandas application
    spark: marks tests as a spark application
    with_spark_context: marks tests as a spark slow application
    without_spark_context: marks tests as a spark slow application